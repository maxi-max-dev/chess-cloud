# 待裁决清单

中途没人可问，下面这些我按任务书自己拍了往下走，都是可以推翻重做的。
每条写清「我选了什么／为什么／要推翻改哪」。

## 1. 页面拆成 3 个文件，不是单个 HTML —— 最需要过目的一条
任务书写「单页 index.html」，我做成了 `index.html` + `engine.js` + `worker.js` 三个静态文件。
**为什么**：铁律里「评估函数第二份 = 全算失败」。页面的分叉排序、cloud Worker 的星点评分、
search Worker 的 minimax 叶子必须共享 `engine.js` 的同一口径。要真塞进一个 HTML，就得把评估函数
源码字符串化再拼 Blob URL 造 Worker，那才是真的坑。仍然是「单页应用、零构建、CDN 引库」。
**推翻的话**：走 Blob URL 方案，我可以改，但代码会难读一截。

## 2. 星数口径：`nodes` 报「当前最深已渲染层」的真实点数
`__cloudStats().nodes` 始终从当前 `cloudState.depth` 对应 geometry 的 `position.count` 现读；
第 4 层分批生长时，它就是当前真实画出的 L4 部分，不会拿 L3 数字或自记计数器冒充。`growing=false`
且 L4 完整结束后，才拿它与 `count(fen, 4)` 做全量对撞。全云总数在 `totalNodes`，每层明细在
`layers`。第 0 层 = 根局面自己，所以 `count(fen, 0) = 1` 也对得上。
**为什么**：`count()` 是 perft 语义（正好走满 depth 步的局面数），只有完整单层才能对撞；
`totalNodes` 是多层求和，不能和 `count()` 混成一个口径。

## 3. AI 思考预算：桌面 1.3 秒，手机/矮视口 0.9 秒
硬指标是「≤3 秒应合法一步」，所以桌面给搜索 1.3 秒预算，手机/矮视口给 0.9 秒，
都用迭代加深，时间到交上一层的结果；主线程另有 2.2 秒 watchdog，Worker 超时或失败就走
预存的合法保底步。星云枚举已全部移出主线程，AI 落子先绘制再延后重建可视化；
新分叉真正就绪前，旧分叉保持淡化、`inert`，确认按钮和事件入口也禁用。
**推翻的话**：想让它更强就加预算，但 3 秒硬指标会变紧，手机端尤其如此。

## 4. 搜索不判和棋
只判将死和困毙，不判三次重复 / 50 步 / 子力不足。
**为什么**：chess.js 的 `isDraw()` 内部会再生成一次走法，等于把每个节点的开销翻倍，搜索深度
直接掉一层。任务书说「棋力业余即可」，我选了深度。
**代价**：残局里它可能以为自己还有戏。

## 5. 第 4 层的困毙局面上色不精确
第 4 层用走法对象自带的 `after` FEN 直接静态评估（省掉约 20 万次 make/undo，是这一层能在
Worker 里分批完成的关键）。将死能认出来（SAN 以 `#` 结尾），但「子局面正好是困毙」认不出来，
会按子力上色。极少数情况，我选了速度。

## 6. 星色刻度 = 一个兵满色（COLOR_FULL=120）
实测起始局面第 3 层 8,902 个分数的分布：min -90 / p05 -55 / p50 +5 / p95 +70 / max +320（百分兵）。
照这个把满色刻度定在 120。刻度定太大（试过 900、220）画面就是一片灰白，「哪片对你有利」白给。
**推翻的话**：改 `index.html` 里的 `COLOR_FULL` 一个数。

## 7. 升变默认成后
没做选棋子的界面。落子入口 `tryMove(from, to, promotion)` 已经收 promotion 参数，随时能接 UI。

## 8. three.js 钉 0.160.0
和诗云同一版（本机验过能用），不追最新（当前 latest 是 0.185.1）。chess.js 钉 1.4.0，任务书指定。

## 9. 名字
仓库名 `chess-cloud`（任务书指定），页面标题现在是「可能性分叉」。正式名仍未定。

## 10. 手机端没适配（已解决，2026-07-30）
现在竖屏按「棋盘 → 分叉 → 星云」纵向排，短横屏/平板用左右双栏；根页面只纵向滚，
分叉留在自己的容器里横滑。星云全屏改由 `#cloudPanel` 本身接管动态视口，修掉了
`backdrop-filter` 让子元素假全屏时产生的偏移暗边和层级覆盖。

缩略星图无论桌面或手机都只保留真实 L3；只有用户明确点「放大探索」才由 Worker 长满 L4，
收起、离屏或切后台都释放 L4 geometry，再次放大时从保存的 L3 FEN 完整重建。
不是只停 rAF 却让 20 万条路径继续占内存。

对应验收已进 `live-check.mjs`：390×844、667/844 横屏、1024 平板、背景首尾像素、
真实页面/分叉滑动、真实 touch e2→e4、667 与 844 全屏探索、四角命中和 DPR backing store
都会检查；L4 首批前模拟切后台也会验证 L3/选路保留和自动续建。

## 11. 深层路径穿越（部分完成，2026-07-31）
全屏路径网会把根局面标成「现在」，并把当前 FEN 的真实 SAN 候选投影在线旁；用户可从标签或候选列表
连续选择三步，亮线、面包屑和右侧 3D 变体棋盘同步更新，且不改变真实对局。第二、第三层标签已经能
真实触摸命中，不再只支持第一层。

仍没做的是从第四层或更深的任意单条路径命中、把它设成新根并继续下。几十万条边不能各配一个 DOM，
后续若做应基于可见层级的拾取/聚类，而且仍要保持显示总数来自 geometry `position.count / 2`。

## 12. 验收脚本 live-check.mjs 收进了仓库
任务书说临时文件放 scratchpad，但 ①② 这两条验收如果只留在我的临时目录里，别人就没法复跑。
所以我把它清掉绝对路径后放进仓库根目录：`node live-check.mjs [--url ...]`，本地和线上都能打。
它只是个 Node 开发脚本，不是构建工具/框架/后端，页面不依赖它。要嫌多余，删掉这一个文件即可。

当前用 `EXPECTED_RESULTS = 83` 把项数固定为 83，少跑一项也失败。历史上的 63/64/70/75/77/82 项
反向验证都保留在 `HANDOFF.md` / `PROGRESS.md`，不要改写旧数字。本轮又分别故意恢复持续 WebGL
重绘、让变体棋盘渲染旧 FEN、在旧布局打开 844×390 全屏、在 L4 首批前触发后台分支，
把 2D 卡片的“走后 N 个分支”故意报大 1，以及先让旧页面缺失 FEN 回合字段；固定裁判准确报红
对应项，恢复正式实现后完整复跑回到 83/83。

## 13. 主分叉默认线 = 两步排序，不是 AI 主变例
主分叉每列挑 `rankMoves(fen, {depth:2})` 的第一名：已经计入对手的一次强回应，不再是旧版的
“贪心一层”。全屏变体探索在根态不伪造默认路线，只有用户点选后才画亮线。
但两者都不是 alpha-beta 搜索得到的主变例，仍可能认不出更深战术。
**为什么不用 AI 的主变例**：那要在 Worker 里再跑一次搜索，会和「≤3 秒应手」抢时间。
**推翻的话**：让 search() 返回主变例（PV），默认线换成「AI 认为接下来会这样走」，
代价是每次落子多一次搜索。这条我觉得值得做，但属于改口径，等你拍。

## 14. 三种“深度”不要混为一谈
主分叉用 `COLS = 5` 展示五个半回合；全屏变体用 `EXPLORE_MAX_PLY = SYNC_DEPTH = 3`
允许交互三步；路径网本身则完整统计到 `DEEP_DEPTH = 4`。旧版的
`PAST_LEVELS / FUTURE_LEVELS` 已删除，不要照历史文档把它们加回来。

## 15. 布局改了，验收②的取样区跟着改了
星云现在只占左边（右边整块给树），所以截图统计从「取整屏中段」改成
「量整朵云、把三块面板挖掉」，阈值也从绝对个数改成占比。
这是量法跟着布局走，不是放水：同一次运行里 ①③ 的对撞数字一个没动。

## 16. 主视图从 3D 星云换成了平面分叉图（2026-07-30，你说「就是一坨」之后）
星云降级成左上角小窗（点开可全屏），主舞台给平面分叉图。
星云本身没删：①② 那两条验收照跑，数字一个没动。

## 17. 「最靠谱」是往前看两步排的，不是引擎全力算的
`rankMoves(fen, {depth:2})`：我走这步 → 对手挑他最好的回应 → 看那个局面值多少。
能认出送子，但认不出更深的战术。开局它把 Nc3/Nf3 排在 e4/d4 前面，
懂棋的人会觉得怪 —— 那是评估函数只有子力+位置表的水平（任务书说「棋力业余即可」）。
**要更准**：第一列改成往前看三步（约 160ms，只在落子后算一次），或者直接调用 AI 的
alpha-beta 搜索给每个候选打分。这条我没自作主张，等你拍。

## 18. 每列先摆几条是写死的
`SHOW = [7, 6, 5, 4, 4]`，`COLS = 5`（往前排 5 步），都在 index.html 顶上。
点「展开其余 N 条」能把某一列整个摊开，展开后顶部可收起；若选了推荐区外的卡，
收起后会额外保留那张已选卡，保证亮蓝主干不断。

## 19. 棋子采用统一 3D 矢量造型，不是第二个可旋转 WebGL 棋盘
旧棋盘根本不是模型，而是黑白双方都用 `♚` 一类黑棋 Unicode 字形，再靠 SVG fill 换色；
Safari/彩色字体回退可能忽略 fill，所以用户端会看到白棋也发黑。

现在六类棋子全部是本地分层 SVG 几何，黑白共用造型，只切换象牙/炭黑渐变、高光、侧面阴影和投影。
这是确定性的分层 SVG 3D 视觉造型，所有棋子统一，也保留正方形格子的准确触摸命中；它不是
WebGL mesh，也没有第二套 WebGL 棋盘、外部模型下载或射线拾取，因此不挤 AI 的 3 秒上限。

若以后要“可旋转棋盘、真实网格厚度、自由相机”的真 3D 棋局，需要单独重做交互、无障碍、移动端
触控和 context-loss 降级，不能把当前棋盘偷偷替换掉。无论走哪条路，黑白真实像素差和 FEN 逐格
对撞都必须继续保留。
