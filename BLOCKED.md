# 设计决策、待裁决与已解决记录

中途没人可问，下面这些我按任务书自己拍了往下走；未标“已解决”的仍可推翻重做。
每条写清「我选了什么／为什么／要推翻改哪」，已解决项保留作历史，不再算待办。项目现在是
根首页选择国际象棋 / 中国象棋，两套规则核分开；旧条目里未特别说明的“页面 / 路径网”指国际象棋页。

## 1. 根入口 + 两个独立页面，不把两套规则塞进一个 HTML（已更新，2026-07-31）
根 `index.html` 现在只放两张选择卡；国际象棋在 `chess.html + engine.js + worker.js`，
中国象棋在 `xiangqi.html + xiangqi-engine.js + xiangqi-worker.js`。

**为什么**：同一棋种的页面排序、Worker 搜索和路径评估必须共享该棋种唯一的 `evaluate()`；同时，
国际象棋的 chess.js / 8×8 / SAN 与中国象棋 9×10 / 炮架 / 马腿本来就是两套规则，强行塞进一个
全局状态只会制造条件分支和竞态。两个棋种各一份评估函数是规则边界，不是违反“同一规则集只有一份”。
仍然是零构建、零框架、零后端的静态站。

**推翻的话**：应先定义明确的 `RulesAdapter / AnalysisAdapter` 契约，并保持两个棋种现有裁判全绿；
不要把源码字符串化拼 Blob URL，也不要让门户承担规则状态。

## 2. 星数口径：`nodes` 报「当前最深已渲染层」的真实点数
`__cloudStats().nodes` 始终从当前 `cloudState.depth` 对应 geometry 的 `position.count` 现读；
第 4 层分批生长时，它就是当前真实画出的 L4 部分，不会拿 L3 数字或自记计数器冒充。`growing=false`
且 L4 完整结束后，才拿它与 `count(fen, 4)` 做全量对撞。全云总数在 `totalNodes`，每层明细在
`layers`。第 0 层 = 根局面自己，所以 `count(fen, 0) = 1` 也对得上。
**为什么**：`count()` 是 perft 语义（正好走满 depth 步的局面数），只有完整单层才能对撞；
`totalNodes` 是多层求和，不能和 `count()` 混成一个口径。

## 3. AI 思考预算：桌面 1.0 秒，手机/矮视口 0.9 秒
硬指标是「≤3 秒应合法一步」，所以桌面给搜索 1.0 秒预算，手机/矮视口给 0.9 秒，
都用迭代加深，时间到交上一层的结果；主线程另有 2.2 秒 watchdog，Worker 超时或失败就走
预存的合法保底步。星云枚举已全部移出主线程，AI 落子先绘制再延后重建可视化；
新分叉真正就绪前，旧分叉保持淡化、`inert`，确认按钮和事件入口也禁用。
**推翻的话**：想让它更强就加预算，但 3 秒硬指标会变紧，手机端尤其如此。

## 4. 国际象棋搜索不判和棋
只判将死和困毙，不判三次重复 / 50 步 / 子力不足。
**为什么**：chess.js 的 `isDraw()` 内部会再生成一次走法，等于把每个节点的开销翻倍，搜索深度
直接掉一层。任务书说「棋力业余即可」，我选了深度。
**代价**：残局里它可能以为自己还有戏。

## 5. 第 4 层的困毙局面上色不精确
第 4 层用走法对象自带的 `after` FEN 直接静态评估（省掉约 20 万次 make/undo，是这一层能在
Worker 里分批完成的关键）。将死能认出来（SAN 以 `#` 结尾），但「子局面正好是困毙」认不出来，
会按子力上色。极少数情况，我选了速度。

## 6. 星色刻度 = 一个兵满色（COLOR_FULL=120）
实测起始局面第 3 层 8,902 个分数的分布：min -90 / p05 -55 / p50 +5 / p95 +70 / max +320（百分兵）。
照这个把满色刻度定在 120。刻度定太大（试过 900、220）画面就是一片灰白，「哪片对你有利」白给。
**推翻的话**：改 `chess.html` 里的 `COLOR_FULL` 一个数。

## 7. 国际象棋升变默认成后
没做选棋子的界面。落子入口 `tryMove(from, to, promotion)` 已经收 promotion 参数，随时能接 UI。

## 8. 国际象棋 three.js 钉 0.160.0
和诗云同一版（本机验过能用），不追最新（当前 latest 是 0.185.1）。chess.js 钉 1.4.0，任务书指定。

## 9. 名字
仓库名 `chess-cloud`（任务书指定），根首页目前用「棋路」，两个棋种页沿用「可能性分叉」。
正式产品名仍未定。

## 10. 手机端没适配（已解决，2026-07-30）
现在竖屏按「棋盘 → 分叉 → 星云」纵向排，短横屏/平板用左右双栏；根页面只纵向滚，
分叉留在自己的容器里横滑。星云全屏改由 `#cloudPanel` 本身接管动态视口，修掉了
`backdrop-filter` 让子元素假全屏时产生的偏移暗边和层级覆盖。

缩略星图无论桌面或手机都只保留真实 L3；只有用户明确点「放大探索」才由 Worker 长满 L4，
收起、离屏或切后台都释放 L4 geometry，再次放大时从保存的 L3 FEN 完整重建。
不是只停 rAF 却让 20 万条路径继续占内存。

对应验收已进 `live-check.mjs`：390×844、667/844 横屏、1024 平板、背景首尾像素、
真实页面/分叉滑动、真实 touch e2→e4、667 与 844 全屏探索、四角命中和 DPR backing store
都会检查；L4 首批前模拟切后台也会验证 L3/选路保留和自动续建。

## 11. 深层路径穿越（部分完成，2026-07-31）
全屏路径网会把根局面标成「现在」，并把当前 FEN 的真实 SAN 候选投影在线旁；用户可从标签或候选列表
连续选择三步，亮线、面包屑和右侧 3D 变体棋盘同步更新，且不改变真实对局。第二、第三层标签已经能
真实触摸命中，不再只支持第一层。

仍没做的是从第四层或更深的任意单条路径命中、把它设成新根并继续下。几十万条边不能各配一个 DOM，
后续若做应基于可见层级的拾取/聚类，而且仍要保持显示总数来自 geometry `position.count / 2`。

## 12. 两套真机验收与门户裁判都收进仓库
任务书说临时文件放 scratchpad，但 ①② 这两条验收如果只留在我的临时目录里，别人就没法复跑。
所以我把它清掉绝对路径后放进仓库根目录：`node live-check.mjs [--url ...]`，本地和线上都能打。
它只是个 Node 开发脚本，不是构建工具/框架/后端，页面不依赖它。要嫌多余，删掉这一个文件即可。

国际象棋 `live-check.mjs` 当前用 `EXPECTED_RESULTS = 102` 把项数固定为 102，少跑一项也失败。历史上的
63/64/70/75/77/82/83/86 项反向验证都保留在 `HANDOFF.md` / `PROGRESS.md`，不要改写旧数字。
此前又分别故意恢复持续 WebGL
重绘、让变体棋盘渲染旧 FEN、在旧布局打开 844×390 全屏、在 L4 首批前触发后台分支，
把 2D 卡片的“走后 N 个分支”故意报大 1，以及先让旧页面缺失 FEN 回合字段；固定裁判准确报红
对应项。一步威胁加入后固定项到 86；本轮再新增 4 项 PV 裁判，覆盖完整搜索主变、零预算保底、
蓝/金路径分离与偏离停止、reset/fallback 清理，以及 390px rail 横滑和静止零持续帧；正式实现
本地完整复跑为 90/90。

中国象棋另有 `xiangqi-verify.mjs`（20 项）、`xiangqi-live-check.mjs`（当前固定 64 项）和
`xiangqi-self-play.mjs`（固定 10 局）；根路由与硬编码红线由 `portal-check.mjs` 当前固定 27 项。
门户裁判曾先对旧单页跑出 8/16，中国象棋真机裁判曾因缩放按钮实际只有 40×44 跑出 30/31；
实现修复后分别是 16/16 与 31/31。视觉 / 威胁层扩验时又先跑出攻击线缺失红灯，最终审查把
“真实动画对象 + 箭头几何 + 多目标切换 + 装饰边界 + 横屏完整首屏”加入后，旧实现为 35/37；
相邻短箭头夹具又先跑出 37/38，动态缩进修复后 38/38。不能为了复用旧 90 项脚本而把两套规则
混进同一个页面钩子。

上一步 / 攻击流扩验又先在旧实现得到 38/43：空起点没有任何视觉、Worker 最新着没有轨迹、
同权箭头缺少“攻→危”方向语义，也没有有限次落子动画。实现后固定 43/43；双攻击者场景同时验证
全部真实线仍在、仅一条主强调且可手动切换，反向污染任一 from/to 都会让同一 Node 真值断言失败。

## 13. AI 主变例叠加到主分叉（已解决，2026-07-31）
`search()` 现在返回最后一层**完整搜完**的单条主变。每一步都有
`san/from/to/promotion/after`；`pv[0]` 必须等于返回的 `move/san`。`depth=0` 只有合法保底着，
`pv=[]`；非终局 PV 长度必须等于完成深度，若搜索线提前走到终局才允许更短。

没有另开一次分析搜索：页面复用刚才黑方应手的同一份 Worker 结果，先从搜索源 FEN 逐手重放校验，
再实际落下 `pv[0]`，把新局面上仍成立的 `pv.slice(1)` 画出来。因此不额外争抢「AI ≤3 秒」预算。

主分叉保留两套独立语义：**亮蓝主干 = 用户当前选路**，**金色虚线/金框 = 引擎主变**。
金色卡即使排在折叠区外也会保留；用户一旦偏离，后续父 FEN 已不同，金线就在真实分歧处停止，
上方横滑 rail 仍保留整条原主变供对照。reset、真实玩家落子、非法/超时 fallback 都会清掉旧 PV。
390px 手机上 rail 只在触摸时横滑，静止后不增加 rAF 或 WebGL 帧。

仍未解决的是 #17：蓝色默认选路继续来自 `rankMoves(depth:2)` 的第一名，金线不会偷偷替用户改选。
这既保留“你选了哪条”的语义，也意味着蓝、金从第一列就可能分叉。

## 14. 三种“深度”不要混为一谈
主分叉用 `COLS = 5` 展示五个半回合；全屏变体用 `EXPLORE_MAX_PLY = SYNC_DEPTH = 3`
允许交互三步；路径网本身则完整统计到 `DEEP_DEPTH = 4`。旧版的
`PAST_LEVELS / FUTURE_LEVELS` 已删除，不要照历史文档把它们加回来。

## 15. 布局改了，验收②的取样区跟着改了
星云现在只占左边（右边整块给树），所以截图统计从「取整屏中段」改成
「量整朵云、把三块面板挖掉」，阈值也从绝对个数改成占比。
这是量法跟着布局走，不是放水：同一次运行里 ①③ 的对撞数字一个没动。

## 16. 国际象棋主视图从 3D 星云换成了平面分叉图（2026-07-30，你说「就是一坨」之后）
星云降级成左上角小窗（点开可全屏），主舞台给平面分叉图。
星云本身没删：①② 那两条验收照跑，数字一个没动。

## 17. 国际象棋「最靠谱」是往前看两步排的，不是引擎全力算的
`rankMoves(fen, {depth:2})`：我走这步 → 对手挑他最好的回应 → 看那个局面值多少。
能认出送子，但认不出更深的战术。开局它把 Nc3/Nf3 排在 e4/d4 前面，
懂棋的人会觉得怪 —— 那是评估函数只有子力+位置表的水平（任务书说「棋力业余即可」）。
**要更准**：第一列改成往前看三步（约 160ms，只在落子后算一次），或者直接调用 AI 的
alpha-beta 搜索给每个候选打分。这条我没自作主张，等你拍。

## 18. 每列先摆几条是写死的
`SHOW = [7, 6, 5, 4, 4]`，`COLS = 5`（往前排 5 步），都在 `chess.html` 顶上。
点「展开其余 N 条」能把某一列整个摊开，展开后顶部可收起；若选了推荐区外的卡，
收起后会额外保留那张已选卡，保证亮蓝主干不断。

## 19. 国际象棋棋子采用统一 3D 矢量造型，不是第二个可旋转 WebGL 棋盘
旧棋盘根本不是模型，而是黑白双方都用 `♚` 一类黑棋 Unicode 字形，再靠 SVG fill 换色；
Safari/彩色字体回退可能忽略 fill，所以用户端会看到白棋也发黑。

现在六类棋子全部是本地分层 SVG 几何，黑白共用造型，只切换象牙/炭黑渐变、高光、侧面阴影和投影。
这是确定性的分层 SVG 3D 视觉造型，所有棋子统一，也保留正方形格子的准确触摸命中；它不是
WebGL mesh，也没有第二套 WebGL 棋盘、外部模型下载或射线拾取，因此不挤 AI 的 3 秒上限。

若以后要“可旋转棋盘、真实网格厚度、自由相机”的真 3D 棋局，需要单独重做交互、无障碍、移动端
触控和 context-loss 降级，不能把当前棋盘偷偷替换掉。无论走哪条路，黑白真实像素差和 FEN 逐格
对撞都必须继续保留。

## 20. 中国象棋没有历史状态，不裁决长将 / 长捉 / 复杂循环
当前 `Position` 只有 `board / turn / halfmove / fullmove`，没有完整着法与局面历史。棋核能准确处理
单局面的走子、将军、将死和困毙，但不能判断循环责任方，也不能按竞赛规则裁决长将、长捉。

曾考虑按 `halfmove >= N` 自动判和，后来删除：这会把一个与历史无关的计数器包装成并不存在的规则
判断，数字虽然整齐，结论是假的。当前页面会明确写出这个范围。

**要解决**：先选定具体规则版本；把可哈希局面、着法性质和重复历史加入真实对局及搜索节点；
新增长将 / 长捉双方责任、允许循环与禁手的定点测试，再扩 `xiangqi-verify.mjs`。在此之前不自动判和。

## 21. 中国象棋是小型本地引擎，不是职业级引擎
`xiangqi-engine.js` 自己实现规则、唯一静态评估、alpha-beta、迭代加深、PV 与浅层分叉排序。
候选会看对方强回应并检查一步合法回吃，足够给 UI 一个可解释的“下一步可能怎么回”，但没有开局库、
置换表、残局库、职业级搜索或真实 WDL。威胁也只是几何攻击线，不是胜率。

**若要更强**：可以研究 Fairy-Stockfish / Pikafish 等，但必须先确认许可、浏览器或 WASM 分发、
Worker 冷启动、包体、移动端性能和无引擎 fallback。接入后要把“规则事实、引擎分数、概率 / WDL”
三种语义继续分开，并守住可见应手 ≤3 秒；不能只换文案就宣称职业棋力。

## 22. 两种棋把“已选择首步”与“未知回应”分开（已解决，2026-08-01）
国际象棋和中国象棋都只把用户明确选择的第一步直接投影到主棋盘。首拍结束后停在
`await-reply`，从真实子局面完整生成对方全部合法回应；金色第一项只是引擎建议，不代表真人或 AI
必然这样走。用户再显式点一个回应，才进入 `conditional-playing` 并停在条件结果。第一步已经终局
时进入 `terminal`，不伪造第二拍。确认按钮始终只提交第一步。

两页共用同形的 DOM 协议：

- 棋盘公开 phase、根 / 展示 FEN、line ID、当前拍和总拍数；`snapshot().frontier` 公开真实回应集合；
- 路径用 `[data-future-preview-step="you"]` / `[data-future-preview-step="reply"]`，运动棋子用
  `[data-future-motion-piece="true"]`；
- 首拍 `playing` 时，回应条以及国际象棋的主 SVG、2D 树、变体棋盘和测试钩子都不能跳进第二拍；
  `await-reply` 后三套可视入口同步解锁，事件入口仍二次守卫；
- 快速换回应会取消旧 timer / animation 并换新 line ID；旧 `animationend` 不能推进新路线；
- 减少动态先静态停在首步，显式回应后才投影两拍条件局面；切回普通动态不补播旧路线；
- 回应列表重建保留手机横向滚动、键盘焦点和“建议 / 已选 / 普通可能”的 ARIA 语义；
- reset、取消或实战根变化会重绘真实根局面，实战 FEN 和历史从不随预演改变。

裁判继续先红后绿：旧门户精确为 **23/24**；旧中国象棋核心新增项为 **51/54**，回应布局污染为
**53/54**；国际象棋旧实现的三条不确定性分支与首拍跨入口旁路都定点报红。其后新增的中国象棋
合法着复用红线曾精确得到 **24/25**，恢复优化后为 **25/25**。最终审查又让 AI 实际应手后的助手
上下文与全景清空焦点各自精确跑红，修复后当前固定基线为门户 **25/25**、国际真机 **101/101**、
中国真机 **58/58**。

## 23. 中国象棋零点击云演与四类身份（已解决，2026-08-03）

P0.3 只改变中国象棋的默认入口：首次加载或 reset 后，候选 Worker 返回就自动选择浅层分析第一名，
再从第二个真实局面接一组分析候选 / 回应，连续演 4 ply 并稳定停住。它不把金色建议改写成预测；
全部真实候选和回应
仍保留，点任一分支会换线接管。国际象棋继续使用 #22 的显式回应流程，没有趁机改成自动选线。

四个标识按不同生命周期使用：`positionKey` 是棋盘布局 + 行棋方的内容键；`positionId` 是真实根
局面的实例号；`requestId` 是候选 Worker 请求；`runId` 是一次预演运行。Worker 回包校验
`requestId + positionId + positionKey`，动画完成回调校验 `positionId + runId`。reset 即使回到
同一个 `positionKey` 也会换 `positionId`，所以旧 run 不可能串入。

自动流程从 `autoplay_started` 起有 8 秒硬预算，每拍严格按“运动完成原子提交 → 落点脉冲 →
威胁展开”推进。暂停、页面隐藏或棋盘离开视口会冻结任务、动画和剩余预算；只在用户明确继续时恢复。
减少动态直接收束为相同 4 ply 静态结果。推演中和结束后常驻“采纳首步继续 / 返回棋盘自己下”，
避免预演只读态遮住实战入口。固定裁判扩为中国棋核 **20/20**、门户 **27/27**、
中国真机 **63/63**。

## 24. 悬停连演不能改写点击、触摸或选路语义（已解决，2026-08-03）

两种棋现在都允许桌面鼠标停在候选上后自动接续一条 4 ply 浅层分析线，键盘 Tab / 方向键聚焦走
同一入口。它只是一种临时预览：路线写入 `preview.path`，`selectedPath` 保持为空，点击仍用于明确
选线或真实棋盘落子；移到另一候选会取消旧线，移出会恢复真实棋盘。

触摸没有 hover，不能拿触屏 click 产生的兼容 mouse / focus 事件冒充桌面悬停。实现同时校验
`pointerType`、pointerdown 抑制窗口和近期键盘导航意图；脚本恢复焦点也不会启动动画。手机画中画
预演棋盘是只读穿透层，不能遮住分叉末列和 PV 横滑。

中国象棋悬停计算不再和完整 44 分支排序共用队列，而使用独立、可终止的 preview Worker。新候选会
直接终止旧 Worker；回包继续匹配 `requestId + positionId + positionKey`，坐标串与索引坐标在 Worker
边界规范化。国际象棋沿用本地浅层排序并以 line token 隔离。固定裁判现为门户 **27/27**、国际真机
**102/102**、中国真机 **64/64**。

## 25. Phase 1 辉光必须是单 Composer、独立真值且能彻底停下（已解决，2026-08-03）

国际象棋只建一个 `EffectComposer`，顺序固定为 `RenderPass → UnrealBloomPass → OutputPass`；云线、
用户选路线与“现在”节点进入 bloom layer，SAN 标签和页面 UI 留在 DOM，不做第二遍全场景渲染。
线云使用 `AdditiveBlending`，让重叠密度自然变亮；bloom 只接住密集峰值，不能把全屏抬成白雾。
当前像素裁判同时设下限和上限：要看得到辉光，又不允许改变超过 25% 画面或让亮部超过 20%。

“现在”节点、后续粒子、光管或涟漪都不得进入 `__cloudStats()`；路径数继续只从真实非索引
`LineSegments.position.count / 2` 求和。`?cinema=1` 只是展示壳，层数行仍来自同一份几何真值。
中国象棋继续使用 SVG / CSS，不得为统一观感引入 Three.js。

Phase 1 没有持续时间轴。普通静置 700ms 后仍须 rAF +0、WebGL frame +0，后台暂停和
`prefers-reduced-motion` 口径不变。后续 Phase 2 若加入流动 / 脉冲，必须由有限活动窗口驱动并在
静置后完全停下；不得以“宇宙应该一直流动”为理由删除零帧裁判。

## 26. Phase 2 只能变形旧云快照，不能改写路径真值或挤占搜索（已解决，2026-08-03）

玩家落子时先把 AI search Worker 派出，再把当前 `layers` 中的真实 LineSegments 转交给一次性的坍缩
快照组。快照保留原 geometry，所以动画期边数守恒；它随即退出 `layers`，新局面的根线仍由既有
`prepareCloud()` 建立，`__cloudStats()` 从来不把快照、金色选线或思考脉冲算成未来节点。

20 万条线的流动、坍缩和重生全部由五个共享 ShaderMaterial uniform 驱动；geometry 只在创建时增加
每条边固定的 `aFlow=0→1` 方向属性，逐帧写入数固定为 0。默认坍缩 600ms，`?collapse=1.2` 只供录屏
慢镜并封顶 1.2s。搜索与坍缩启动实测相差约 1ms，搜索不等待动画；思考脉冲在搜索结束立即销毁，
新云再从所选端点重生。

IntersectionObserver 的初始样本在无头浏览器可能早于固定面板完成布局，因此每次请求绘制都会用实时
DOM 矩形复核一次；真实离开视口仍暂停，首个零尺寸伪样本不会把整段叙事永久冻住。页面隐藏、切到
2D、离开视口会冻结时钟；恢复时整体平移时间戳，不跳帧追赶。减少动态直接定格，静置继续要求
rAF / WebGL frame +0。

## 27. Phase 3 视觉对象必须服从运动提交顺序且不进入棋局真值（已解决，2026-08-03）

选中呼吸、落点涟漪、吃子碎裂、面板扫描和路径传导都是临时视觉对象。棋子数仍从实战 FEN 与棋盘
DOM 对撞，路径数仍从真实路线读取；10 粒碎片、涟漪和扫描节点不能进入任何棋子 / 分叉 / 云线计数。
效果结束后必须实际移除，而不是只把 opacity 设为 0 留下幽灵节点。

AI Worker 可以和玩家位移动画并行搜索，但提前返回的应手不能覆盖仍在运动的玩家棋子。当前协议会
暂存同一 request 的结果，在已校验的运动 id / 当前 FEN 完成后再落盘；reset 会同时换请求代际并清掉
暂存结果。中国象棋的吃子语义从走前 FEN 的落点现读，不要求修改棋核或 Worker 消息。

呼吸只播放一个 2 秒周期，扫描 200ms、路径传导 300ms、涟漪 400ms、碎裂 500ms，均为有限一次；
减少动态只保留静态环和路线语义。悬停逐拍路径继续属于 preview，`selectedPath` 必须为空，不能因为
动画更醒目就把临时路线升级为用户选择。

## 28. “正在看的局面”可以成为视觉根，但不能冒充实战现在（已解决，2026-08-04）

国际象棋 3D 全景拆分两个根：`game.fen()` 是实战根，`explorerFen()` 是条件分析焦点。跟随模式允许
后者成为 WebGL 云线的真实展开根，因此 `__cloudStats().fen` 会随选路变化；同时必须公开并保留
`liveFen`，右侧棋盘明确写“走后即时评估 · 不改变实战”，过去路径首节点叫“实战现在”，中央根叫
“正在看的局面”。只有真正落子才允许改变 `game.fen()` 和 history。

每次焦点变化都换 `cloudState.gen` 并终止旧 Worker，回包继续同时校验 Worker 对象和 generation。
页面隐藏时不得为了新的焦点先清空已完成 L3：只设置 deferred，回到前台再按最新焦点一次重根；
快速连续选路同样只有最后一代可写入 geometry。

标签可以在 L1 geometry 到达前用确定性空间坐标先出现，以保证连续选择不被深层计算卡住；但它们是
DOM 交互层，不是路径节点，不得进入 `__cloudStats()`。真正的 L0–L4 数量仍只从非索引
`LineSegments.position.count / 2` 读取，“全局总览”也必须回到实战 FEN 后重新对账。
