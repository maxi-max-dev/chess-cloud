# chess-cloud 进度

线上入口：https://maxi-max-dev.github.io/chess-cloud/ ｜ 当前本地版本已经是双棋种门户：
根 `index.html` 先选国际象棋 / 中国象棋，原国际象棋应用迁到 `chess.html`，新增
`xiangqi.html + xiangqi-engine.js + xiangqi-worker.js` 和独立验收。最新本地固定基线为
国际棋核 8/8、中国棋核 19/19、门户 16/16、国际真机 90/90、中国真机 43/43；详见文末与
`HANDOFF.md`。下面任务 0–11 是国际象棋的历史演进，任务 12 是双棋种更新。

## 我理解的目标（开工前写，≤10 行）
1. 目标：网页版国际象棋，人执白落子后，把「这步之后所有可能未来」炸成三维星云；星色=局面评估（白优暖/黑优冷）；AI 执黑陪下。
2. 让步顺序：算得对 > 看得震撼 > 功能全 > 加载快。数字对不上就是失败，宁可少画也不许假数。
3. 顺序：任务 0 环境核对 → 1 棋核+verify.mjs（冻结）→ 2 星云+__cloudStats → 3 上线 Pages 并在线上重跑验收。
4. 最大风险：__cloudStats().nodes 必须等于 count(fen,depth)，且必须读 three.js 几何体真实边数。
   对策：每批路径用「精确大小」的非索引 LineSegments，nodes = 所有几何体 position.count / 2 求和，绝不自记计数器；
   预分配+drawRange 一律不用（那等于自记计数器）。
5. 次风险：4 层 197,281 节点同步算会卡死 8 秒 → 必须真 Worker 分批流回；AI 用迭代加深+时间预算保证 ≤3s 应手。
6. 第三风险：评估函数只允许一份 → 抽成 engine.js，页面分叉、cloud Worker 星色和 search Worker
   的 minimax 叶子都共享同一套 evaluate() 口径。

## 任务进度
- [x] 任务 0 环境核对
- [x] 任务 1 棋核 + verify.mjs（已冻结）
- [x] 任务 2 可能性星云 + __cloudStats
- [x] 任务 3 上线 GitHub Pages + 线上重跑验收
- [x] 任务 4 手机端适配 + 星云全屏背景修复
- [x] 任务 5 分叉路径 + 可探索文字星图
- [x] 任务 6 统一 3D 矢量棋子 + AI/重开竞态加固
- [x] 任务 7 零光点路径网 + 棋子助手 + 人话评价
- [x] 任务 8 路径网降载 + 可操作的真实变体棋盘
- [x] 任务 9 3D/2D 真树切换 + 分叉数字 + 手机可读/低负载
- [x] 任务 10 修复 2D 全景树永远显示“第一步”
- [x] 任务 11 搜索 PV + 蓝色用户路径/金色引擎主变
- [x] 任务 12 双棋种门户 + 中国象棋规则核 / Worker / 真机验收

---

## 任务 0 环境核对（实测）
- `node -v` → v22.23.1（/opt/homebrew/bin/node）
- `npm i chess.js@1.4.0` → added 1 package；node_modules/chess.js 的 version 字段 = 1.4.0
- `node -e "new Chess().moves().length"` → **20** ✅ 和任务书一致
- `gh auth status` → ✓ maxi-max-dev 已登录，scopes 含 repo ✅
- 结论：环境和任务书完全对得上，没有需要停工的偏差。

## 任务 1 棋核 + verify.mjs
`node verify.mjs` → 8/8 全绿零跳过（20 / 400 / 8902 / 197281，外加 Kiwipete 48 / 2039 / 97862，
以及 count(fen,0)=1 的约定）。Kiwipete 那三档是我自己加的：它含双方易位、吃过路兵、升变分支，
能证明 `count()` 对任意 FEN 都对，而不只是对着起始局面那几个数字。

**反向验证（已跑）**：把 `expect: 197281` 改成 `197282` → `红 ✗ 起始局面 4 层：实得 197281，期望 197282`，
退出码 1；`git checkout -- verify.mjs` 改回 → 8/8 全绿，退出码 0，`git diff verify.mjs` 零行。

## 任务 2 可能性星云
- 当前最终架构里第 1～4 层棋局展开全部在专用 cloud Worker：第 1～3 层逐层整批回传，
  第 4 层按父节点边界分批回传；主线程只创建真实 Three.js geometry，不再同步枚举棋局。
- 展开走的是 chess.js 的 verbose 走法：走法对象自带 `after`（子局面 FEN）和 `san`，一次走法生成
  就同时拿到子局面和分数，比 make/fen/undo 少一次重复状态转换。
- 星数口径见 BLOCKED.md 第 2 条。每批一个「刚好这么大」的 BufferGeometry，`position.count` 就是真实星数。

**验收（无头 CDP，本地 + 线上各跑一次，都 20/20 全绿）**
- ① `__cloudStats().nodes == count(同 fen 同 depth)`：起始局面 197,281 对上；**AI 应手后的局中局面
  475,842 也对上**（这个数字任何地方都没写死，只能是真算出来的）。每一层都单独对：1/20/400/8902/197281，
  局中 1/30/654/20144/475842。
- ② 截图 `scratchpad/cloud.png`（线上版 `live-cloud.png`）：中段亮像素占比 0.399，很亮的 3,736 个；
  暖色 8,035 个、冷色 19,231 个 —— 两色都真的看得出来，不是一片灰白。
- 附带：非法走子被拒（e2→e5 / a1→a3 / 动黑子，三个全 false 且 FEN 不变）；e4 后 AI 应手外部秒表
  1,957ms（页面自计 1,547ms，搜到 4 层）；AI 那步在 node 端用 chess.js 独立重放，FEN 一致。

**反向验证（已跑）**：临时把第 4 层改成只画一半点 → ① 变红（页面 98,635 vs count() 197,281，
局中 237,907 vs 475,842），且前三层仍绿=定点失败；`git checkout -- index.html` 改回 → 20/20 全绿。

## 任务 3 上线
- `gh repo create maxi-max-dev/chess-cloud --public` → https://github.com/maxi-max-dev/chess-cloud
- Pages 开在 main / 根目录，`curl -sI` → `HTTP/2 200`；三个静态文件 index.html / engine.js / worker.js 各 200。
- 线上页用同一套脚本重跑：20/20 全绿（含 e4 后 AI 1,957ms 应手 + ①② 全过）。

## 任务 4 手机端适配
- 竖屏恢复正常文档流，顺序为棋盘 → 分叉 → 星云；短横屏/平板用左右双栏。
- 分叉 SVG 保留 5 列和真实卡片数，只在自己的容器内横滑；触控目标提升到至少 44px。
- 手机/矮视口的 AI 搜索预算收紧到 0.9 秒，给 DPR=2 渲染、路径标签和触控留余量，移动验收仍硬卡总应手 ≤3 秒。
- 星云全屏由 `#cloudPanel` 接管整个动态视口，避开 `backdrop-filter` 对 fixed 子元素的定位和层级限制；
  同时补齐安全区、竖屏相机取景、DPR resize、触控取消。
- `live-check.mjs` 新增 8 项手机验收，总数 33 项。

**反向验证（已跑）**：先只加入手机验收、不加响应式修复，旧页面 33 项里 6 项变红：
竖屏裁切、触控目标过小、分叉不可达、背景边缘异常、星云假全屏、短横屏重叠。
最终又临时禁掉页面纵滑、分叉横滑和星云 pointer events，新版验收定点出现 4 个红项；
删除临时破坏后同一套验收恢复 33/33 全绿。发布前又把 667/1024 的“双栏”改成显式几何断言，
临时让 667 横屏规则失效时该项单独变红（1/33），恢复后重新全绿。`verify.mjs` 未改。

## 任务 5 分叉路径 + 可探索文字星图
- 「展开其余 N 条」现在能原位收起；展开态把收起入口留在列表顶部。选择推荐区外的走法会自动收起，
  但已选卡仍留在 DOM，主干不会断。
- 分叉图新增贯穿五列的亮蓝主干、逐列选中边与卡片标记；触屏选路后自动把下一列带入视野。
- 星图第 0 层仍是唯一真实根点，并标作「现在」；第一层从 360° 球壳改为朝未来方向张开的锥形树。
- 全屏星图只投影少量真实 SAN 标签，亮线沿真实 parent/FEN/position 索引穿过同步前三层；
  点第一层标签会同步右侧整条路径但不落子，拖动时标签重排、远处星点受 fog 淡化。
- 显式 44px「收起」按钮取代全屏空白误触关闭；预览装饰不再截获棋盘触摸。
- `aiRequestId` 先让 reset 作废途中 Worker 回包；任务 6 又补上 terminate/recreate，避免旧搜索继续占队列。
- `live-check.mjs` 扩到 43 项，覆盖真实展开→收起、推荐区外选路、连续主干、根点/前向几何、
  多步合法路径标签、触摸选路、相机拖动显隐、按钮收起和 AI/reset 竞态。

**反向验证（已跑）**：临时同时破坏前向锥、AI id 校验、收起状态和多步路线，
完整验收出现 7 个红项（7/43）；恢复实现后同一套验收回到 43/43 全绿。`verify.mjs` 未改。

## 任务 6 统一 3D 矢量棋子 + AI/重开竞态加固
- 删除系统 Unicode 棋子字形。六种棋子全部改为本地分层 SVG 几何，黑白双方共用造型，
  分别使用象牙/炭黑渐变、高光、侧面阴影和底座投影；Safari 或彩色字体回退不再参与渲染。
- 这里的“3D”是统一、确定性的分层 SVG 立体视觉造型，**不是**可旋转的 WebGL mesh 棋盘；
  没有外部模型下载、第二套相机或射线拾取。
- `window.__test.board()` 从实际 `g.piece3d` 读取格位、阵营、类型、材质、图元和屏幕边界；
  `live-check.mjs` 用 Node 端 FEN 逐格对撞，并以“正常截图－隐藏棋子层截图”取得真实像素掩膜。
- 修掉重开只丢回包、不终止同步 Worker 的队列问题；reset 现在终止并重建搜索 Worker。
- 搜索 deadline 从每 1024 节点检查改为每 32 节点，并在根候选/节点内部检查；主线程增加
  2.2 秒 watchdog，Worker 超时、异常或通信失败时用预存合法步保底。
- 星云第 1～4 层全部迁入 cloud Worker。手机星图屏外只保留真实 L3，进入视口才长满 L4；
  离屏会释放 L4，回屏再从 L3 FEN 完整重建，不能拿截断层冒充全量。
- AI 应手先落子并让棋盘/状态真正完成一帧绘制，再延迟计算最终分叉和星云。等待期间旧分叉保持淡化，
  `forkWrap.inert = true`，确认按钮和事件入口也一起封住，不能误选旧路径或偷跑 `rankMoves()`。
- reset 同时清空旧状态文案、走法详情、AI 结果和分叉横滚；离屏星云不再保留最重的 L4 geometry。
- 验收脚本的本地服务和 CDP 默认改为动态空闲端口；显式端口已占用会直接拒绝，避免误连别的 Chrome。
- `live-check.mjs` 用 `EXPECTED_RESULTS = 64` 固定为 64 项，覆盖棋子逐枚/逐类型像素、特殊走法、
  引擎 deadline、AI 真实绘制、旧分叉 inert 和移动星云 L3→L4→L3→L4 生命周期。

**反向验证（已跑）**：在验收脚本还是 63 项时，同时故意破坏白方单类材质、引擎 deadline、
Worker/星云生命周期和旧分叉隔离等关键路径，完整验收准确出现 **10/63 项失败**。恢复实现后才新增
固定项并把 `EXPECTED_RESULTS` 锁到 64；`verify.mjs` 未改。
最终又针对新补的黑棋可见轮廓与手机精确层集合做反向验证：临时让黑象只剩公共底座，并仅在手机端
删掉第 2 层，固定套件准确出现 **6/64 项失败**；恢复后完整复跑才回到 64/64。

**本地完整复验（2026-07-30，恢复正式实现后）**：
- `node verify.mjs` → **8/8 全绿，零跳过**。
- `node live-check.mjs` → **64/64 全绿**。
- AI 可见应手：首屏云并发 1,554ms；4× CPU 慢速手机 948ms；reset 后新请求 1,629ms；
  强杀搜索 Worker 后合法保底 2,250ms；正常桌面 1,538ms；真实手机触摸 967ms，全部低于 3 秒。
- 正预算 deadline 探针 140.4ms；临时关掉周期查钟后变成 210.7ms，新 200ms 门槛按预期报红。
- 桌面截图白/黑棋中位亮度 195.7/57.4，逐类型最小差 122.9；最近两类轮廓差仍为 0.055，
  证明不是仅换 `data-*` 属性或六类共用同一轮廓。

## 任务 7 零光点路径网 + 棋子助手 + 人话评价

- 删除星点贴图和所有 `THREE.Points`；每个未来节点改为恰好一条父→子非索引 `LineSegments` 入边，
  数量从真实 `position.count / 2` 现读。根用“过去→现在”短边，首层和后续收窄成向右生长的知识路径网。
- 深层用 NormalBlending 和逐层透明度，L4 仍逐条绘制。新增“正常 / 隐藏全网 / 单独隐藏 L4”
  PNG 对撞，证明最深层不是只耗内存却不可见；移动离屏还会从 scene 真正释放 L4。
- 全屏保留真实 SAN、局势和当前路线文字；fog 与缩放上限对齐，标签进雾会隐藏，并增加“回到全景”。
- 点白棋后只展示该棋子前三候选；黄线演示你的一步，蓝线演示 `rankMoves(after,{depth:1})`
  的对手强回应。卡片主信息改为均衡/稍优/风险等人话，raw score 作为次要核对。
- 实际 AI 回来后对照预测；fallback 展示真实原因。补齐黑方质量方向、默认升后、一步终局、
  AI 后 120ms 分叉刷新竞态、旧动画清理和键盘入口。
- `live-check.mjs` 固定扩为 70 项。反向验证临时隐藏真实 L1 并反转回应排序，两项裁判均准确报红；
  删除临时破坏后才运行最终完整绿套件。`verify.mjs` 未修改。

## 任务 8 路径网降载 + 可操作的真实变体棋盘

- 缩略路径网固定只建真实 L3；只有用户明确点「放大探索」才继续在 Worker 算 L4，收起、离屏或切后台
  都释放 L4。L4 分批完成后合并成单一精确 geometry，L0–L4 各一个对象，拖动不再背着上百 draw calls。
- WebGL 改为事件驱动，默认巡航关闭；静置 700ms 实测 rAF +0、WebGL frame +0、pending=0。
  主动巡航约 24fps，收起立即停。缩略 DPR 1，全屏桌面封顶 1.5，粗指针/短边设备封顶 1.25；
  加上 `ResizeObserver` 与 `visualViewport.resize`。
- 三路并行冷启动复审曾跑出 3,050ms / 4,785ms 两次红线，桌面 AI 预算因此从 1.5s 收紧到 1.3s，
  给 Worker 启动、调度和首屏资源竞争留出更多余量；没有改验收的 3 秒上限。修复后又同时启动两套
  独立浏览器，冷启动实测 2,718ms / 2,564ms，双双在线内。
- hover 只重画 overlay，不再为一次预览重建 32 枚棋子；助手黄/蓝动画都变成 1.5 秒单次播放。
  切后台保留完整 L3 和当前变体路径，不再无条件整树重算。
- 全屏右侧/下方新增真实 3D 矢量变体棋盘、面包屑和当前 FEN 的全部合法走法。可从图上标签或列表
  连续探索三步；线、标签、面包屑、棋盘都来自同一条 `after` FEN 链，真实实战 `game` 完全不动。
  即时 depth=1 与“含对手强回应” depth=2 显式标出评估视野。
- 手机补齐候选真实滑动、所有关键入口 ≥44px 与中心命中；667×375 放大探索用专门双栏布局，
  390×844 可连续触摸两层图标签并逐格核对棋盘。
- `live-check.mjs` 先扩为 75 项。反向验证一：故意恢复持续重绘，准确报红
  `rAF +15 / WebGL +15 / pending=1`；反向验证二：故意让变体棋盘画旧 FEN，连续两步逐格同源断言
  准确报红。随后再补 L4 首批前切后台和 844×390 全屏两项：旧实现分别报红 pending 丢失、
  236px 棋盘与四类不可命中目标；固定项最终为 77，恢复后完整复跑 77/77。`verify.mjs` 未修改。

## 任务 9 3D/2D 真树切换 + 分叉数字 + 手机可读/低负载

- 3D 区新增「切到 2D 树」；缩略态直接展示当前层的真实走法卡和“走后 N 个分支”，全屏则从
  「现在」向下展开最多三层。每层只渲染当前选中父节点的全部合法孩子，不复制整棵 L4 到 DOM。
- 每张卡的分叉数从 `move.after` 重新生成合法走法现读；根数、层内完整 move set、卡片 SAN/FEN、
  走后分支数均由 `live-check.mjs` 在 Node 端逐节点独立重放。根→选中卡之间是真实 SVG 连续主干。
- 2D 与 3D 复用同一 `explorerState`，旧层换兄弟、2D→3D→2D、L3 尚未回传时快速切换都保留路径；
  右侧变体棋盘始终逐格显示同一 FEN。横排分支可用左右箭头/横滑，整树可纵滑，箭头边界从真实
  `scrollWidth/clientWidth/scrollLeft` 决定。
- 2D 会释放 L4、取消巡航与待绘帧；放大/收起不为隐藏 canvas 调 `setSize`。实测 700ms：
  rAF +0、WebGL +0，隐藏 backing store 像素尺寸不变。
- 390×844 真触摸可横滑、纵滑并点满第三步；全屏树在上、变体棋盘在下且不遮挡。
  1440×900 / 1280×800 面板间距均为 8px；模拟 47px 刘海、34px 底部手势区后仍留 10px 间隔。
- AI thinking 时树和变体棋盘同时 `inert/aria-busy` 并清掉旧卡；应手真实绘制后才按新 FEN 重建。
- `live-check.mjs` 固定扩到 82 项。反向验证把卡片分叉数临时改成 `replies + 1`，裁判逐节点准确报红
  `L1 Nc3 走后分支 21≠20`；恢复实现后完整复跑 82/82。`verify.mjs` 未修改。

## 任务 10 修复 2D 全景树永远显示“第一步”

- 真实浏览器连续走完两回合后确认：根 FEN 和分支数一直正确更新，但层头仍把相对深度 1 裸写成
  “第 1 步”，所以视觉上像棋局没前进。
- 新增 `fenTurnContext(fen)`，直接从父 FEN 的 side/fullmove 得到已走手数、绝对 ply、真实回合和
  行棋方。内部 `depth=1..3` 保持相对索引；界面主标题改为“第 N 回合 · 白/黑方走”，副标题明确
  “未来第 1/2/3 步”，根卡和两个面包屑都写当前已走手数/真实回合。
- 手机 390×844 实测两层标题与 44px 左右箭头不重叠；右侧变体棋盘同步显示相同回合面包屑。
- `live-check.mjs` 固定扩到 83 项：真实完成一回合必须显示第 2 回合；另外加载
  fullmove=37、黑方走且 history 为空的 FEN，仍必须显示第 37 回合，证明没自记历史。
- 反向验证先只加裁判，旧实现准确报红根/层回合字段缺失；恢复正式实现后完整复跑 83/83。
  `verify.mjs` 未修改。

## 任务 11 搜索 PV + 蓝色用户路径/金色引擎主变

- `search()` 的 negamax 新增三角 PV 表，只在当前迭代层完整结束后把结果公开。每手固定带
  `san/from/to/promotion/after`，首着必须等于 `move/san`；非终局 PV 长度等于完成深度，
  提前终局可缩短。零预算保底结果明确是 `depth=0,pv=[]`，不把未搜索走法包装成主变。
- 页面不启动第二次分析搜索。它复用上次黑方 Worker 搜索的完整 PV，从搜索源 FEN 逐手核对合法性、
  SAN 和 `after`，实际落下黑方 `pv[0]` 后，把新局面仍成立的 `pv.slice(1)` 投影到右侧分叉。
  这条数据链没有增加 AI 的搜索预算，也不与 ≤3 秒应手争抢另一个 Worker。
- 分叉保留两套独立语义：亮蓝主干继续表示用户当前选路，金色虚线/金框卡片表示引擎主变。
  金卡在折叠区外仍保留；用户偏离后，后续父 FEN 已改变，金线在真实分歧处停止，上方 rail
  继续保留完整主变供对照。reset、真实玩家下一步和 Worker fallback 都会清空旧 PV。
- 390px 手机把 PV 做成面板内横滑 rail，不撑宽根页面；静止后 rAF pending=0、WebGL 帧不增长。
  `self-play.mjs` 也逐次独立重放搜索 PV，报告新增 `pvFailures`，任何非 0 都退出 1。
- 最新 `node self-play.mjs` 实跑 10 局、621 plies / 581 次 search：
  `illegalMoves=0`、`fenMismatches=0`、`pvFailures=0`，89 条注释抽样失败 0，总耗时 13.78s。
- `live-check.mjs` 固定扩到 90 项：完整搜索 PV、零预算空 PV、固定局面的
  `pv.slice(1)`→rail/金卡/金边同源、蓝金分离与偏离停止、reset/fallback 清理、手机横滑与零持续帧
  都有独立断言。本地完整复跑 90/90，`verify.mjs` 未修改。

## 任务 12 双棋种门户 + 中国象棋

- 原国际象棋单页用 `git mv` 迁到 `chess.html`，根 `index.html` 改成轻量选择页，恰好两张卡：
  `./chess.html` 和 `./xiangqi.html`。国际 `live-check.mjs` 的本地入口同步改为 `chess.html`，
  原 90 项完整回归仍为 90/90。
- 新增中国象棋 9×10 规则核，不依赖 chess.js 换皮。`xiangqi-engine.js` 覆盖 FEN 往返、
  将 / 帅九宫与照面、士、象眼与不过河、马腿、车、炮架、兵卒过河、己方被将过滤、将死和困毙。
  `evaluate()` 在这套规则内保持唯一，正=红优；搜索为带时限的 alpha-beta / 迭代加深并返回可重放 PV。
- 起始局面 perft 用独立裁判确认：
  **44 / 1,920 / 79,666 / 3,290,240**。`xiangqi-verify.mjs` 固定 19 项，还覆盖攻击 / 保护、
  非法着、零预算合法 fallback + 空 PV，以及候选看对方强回应和一步合法回吃。
- 中国象棋页面为真实 90 点棋盘、32 枚棋；人执红、AI 执黑。右侧纯 2D 树从当前局面渲染全部合法
  分叉，每个节点写真实后续分叉数。点卡片只预演蓝色“1”与金色“2”，不改 FEN；点击棋盘蓝色落点
  才真正走。搜索完成层的 PV 单独显示，不说成概率。
- 页面使用两个 `xiangqi-worker.js` 实例：`searchWorker` 专注 AI，`branchWorker` 计算当前所有候选；
  reset 同时 terminate / recreate，避免旧同步任务排队。页面搜索预算 1.1 秒、watchdog 2.8 秒，
  Worker 失败只走预存合法着，`depth=0,pv=[]` 不冒充搜索。
- 威胁注释只表示对方几何攻击线，保留攻击者 / 保护者，不声称下一手必吃或胜率。
  `Position` 目前没有历史，明确不裁决长将、长捉和复杂循环，也没有用 `halfmove` 阈值伪造和棋。
  当前是可解释、可验收的小型本地引擎，不是职业级中国象棋引擎。
- 新增 `portal-check.mjs` 固定 16 项；先对旧单页运行得到 8/16，确认两个新入口和独立规则契约真的会红，
  实现后恢复 16/16。`xiangqi-live-check.mjs` 固定 31 项，首次真实测到缩放按钮 40×44，
  得到 30/31；改成 44×44 后完整 31/31。
- 中国象棋真机验收还覆盖：根首页实际点击、32/16/16 与 90 点、起始 44 张卡及逐卡 FEN / 分叉
  Node 重放、真实走两回合后路径从 ply 0 前进到 4、威胁固定局面、缩放、reset 竞态、
  强杀 Worker fallback、1440×900 / 390×844 / 667×375、根页面无横向溢出、控件 ≥44px、
  静止零 rAF 和零 JS 错误。
- 最新 `node xiangqi-self-play.mjs` 实跑 10 局、769 plies；6 局真实终局、4 局达到 100 plies 上限；
  `illegalMoves=0`、`fenMismatches=0`、`pvFailures=0`、`branchFailures=0`、`threatFailures=0`，
  单次搜索最大 38.9ms。它证明稳定性，不证明棋力。

## 任务 13 中国象棋视觉与威胁可读性

- 先对照 Xiangqi.com、天天象棋、PlayStrategy、Just Xiangqi 和开源分析盘。共同有效的不是堆装饰，
  而是浅木棋盘、大棋子、红黑高对比，以及把选中 / 合法点 / 上一步 / 可吃 / 受攻做成不同形状。
- 棋盘改为克制的现代木盘，补齐炮位 / 兵位折角标；棋子改为带倒角、内圈和短软阴影的象牙圆子，
  路径面板同步收拢为墨绿 / 黄铜色。390px 视口的最小棋子从约 29px 提到实测 33.2px，
  最大字盘比为 0.76；667×375 使用双栏，棋盘实测 278×309 并完整进入首屏。
- 页面不再把 `getThreats()` 压成 `Set`。现在保留每个目标的 `attackers / defenders / value /
  certainty`，棋格的 `data-attackers / data-defenders / data-threat-level` 与棋核逐项同源。
- 受攻目标常驻双层危险环和“危”字；默认展示最高优先级攻击线，点受攻棋或威胁条可切换真实
  attacker → target SVG 箭头。说明条写明坐标、攻守数量和保护者，并继续限定为“当前几何攻击线”。
- 动效只在新风险 / 新箭头出现时有限次播放，2.6 秒后实测运行中的动画为 0；没有 rAF 循环。
  `prefers-reduced-motion` 下 `animation-name=none`，静态环、箭头和文字仍保留。
- `xiangqi-live-check.mjs` 从固定 31 项扩到 38 项。第一轮新增断言先在旧页面跑出攻击线等待超时，
  确认真会红；实现中间态以 35/36 精确抓出手机字盘比例。审查又发现危险环会被连续同步 render
  在首帧前销毁，于是把裁判加强为必须同时出现真实 `animationName` / Web Animation 对象，
  并核对箭头坐标、线长、方向、marker 和可见性；旧实现按预期为 35/37，修复后 37/37。
  第二次审查用相邻 `e5→e4` 找到固定缩进会让短箭头反向，新增夹具先得到 37/38；改为按距离
  动态缩进与紧凑 marker 后为 38/38。最终还覆盖多威胁逐项切换、390px 棋子 / 环 / 徽标边界
  与 667×375 棋盘完整进入首屏。
- 本轮复验：`npm test` 为 8/8 + 19/19 + 16/16，国际 `live-check.mjs` 90/90，中国
  `xiangqi-live-check.mjs` 38/38；中国象棋自走 10 局 / 844 plies，4 终局 / 6 capped，
  五类错误全 0，最大搜索 23.0ms。

## 任务 14 中国象棋上一步与攻击流

- 旧实现虽然保存了 `lastMove`，却只给 `.last-from .piece` 加阴影；落子后起点已经为空，所以
  起点实际上永远不可见，终点也只有很淡的金圈。现在使用独立 SVG 层持续显示空心起点、金色实线、
  落点环和“上一步”标签，事实条同时显示行棋方、真实记谱与起终坐标。
- 每次真实落子只播放一次棋子位移、轨迹划入与落点收束；后续同步 `render()` 不会重播。悔棋只恢复
  静态上一着，重开 / 测试装载会清空一次性 token。所有动效只改 `transform / opacity /
  stroke-dashoffset`，有限次结束后页面回到零持续帧；在减少动态状态落子后切回普通，也不会补播旧着。
- 威胁不再让所有箭头同权挤成难读的 V。全部真实攻击线仍在 DOM 中，但只有当前攻击者是 0.94 主线，
  其他线降为 0.22 上下文；来源写“攻”、目标写“危”，短流光明确方向。多攻击者用真实 `1/N`
  按钮切换，不自动循环，也不删掉其余攻击者。
- `xiangqi-live-check.mjs` 从 38 项扩到 43 项。新增裁判先在旧实现上精确得到 **38/43**：
  Worker 最新着没有轨迹、固定夹具没有起终标记 / 文字、没有“攻→危”流光、没有有限落子动画，
  反向坐标污染也无法建立真值基线。实现后 **43/43**，并用双攻击者夹具验证主线
  `0.94`、背景线 `0.22` 和 `a4 → e9` 手动切换；坐标仍由 Node 棋核独立重放。
- 发布前额外发现 `self-play.mjs` 还在读取门户化之前的 `index.html`。只把 UI 文案函数的来源修正为
  `chess.html`，没有改任何局面、分数或失败条件；修复前脚本定点报错，修复后固定 10 局共
  631 plies，五类错误汇总全为 0。

## 完成条件核对
- 门户条件 ✅ 根首页恰好两张棋种卡，规则代码分别留在 `chess.html` / `xiangqi.html`，没有混进门户。
- 国际架构 ✅ 六种分层 SVG 3D 棋子（非 WebGL mesh）；路径网 L1–L4 全在 Worker；AI 先真实绘制
  再延迟可视化；旧分叉 inert；缩略只保留 L3，明确放大才加载 L4，收起释放。
- 中国架构 ✅ 9×10 独立规则核；search / branch 两个 Worker；真实 2D 分叉、PV、威胁与手机布局。
- 裁判原则 ✅ 没有放宽 `verify.mjs`；新增行为由中国棋核、门户和中国真机三个独立裁判覆盖，
  运行代码硬编码检查包含两套页面 / 引擎 / Worker。
- 本地复验 ✅ `npm test` 为 **8/8 + 19/19 + 16/16**；
  `live-check.mjs` **90/90**；`xiangqi-live-check.mjs` **43/43**。
- 国际稳定性 ✅ 固定 10 局 631 plies，异常局 / 非法着 / FEN / PV / 注释失败全为 0。
- 稳定性 ✅ 中国象棋固定 10 局 844 plies、4 终局 / 6 capped，五类错误全 0，最大搜索 23.0ms。
- 诚实边界 ✅ 中国象棋没有历史状态，不伪造长将 / 长捉 / 循环裁决；威胁只写几何攻击线；
  不冒充职业级引擎。
- 发布状态 ✅ 上一步 / 攻击流运行版本 `66f5a92` 已推到 `main`；七个运行文件与 GitHub Pages
  SHA-256 逐字节一致；中国象棋线上 **43/43**，国际象棋线上 **90/90**。
- `BLOCKED.md` 现保留 21 条设计记录；#20 / #21 记录中国象棋规则与棋力边界。

## 期间为什么改过方向（如实记录）
1. 第一版星云颜色全糊成白。原因两条：均势色被我调成偏蓝的（b 比 r 高 41），导致「黑优」和「均势」
   在画面上根本分不开；满色刻度定在 900 百分兵，而实测起始局面第 3 层 8,902 个分数的分布是
   min -90 / p05 -55 / p50 +5 / p95 +70 / max +320 —— 刻度比信号大一个数量级，颜色白给。
   改成真中性灰 + 刻度 120（一个兵），并把这条分布打印进验收脚本，刻度是照数据定的不是拍脑袋。
2. 第一版形状是个毛球：锥角 0.74 太宽、步长衰减太慢，20 条主枝糊成一层层壳。收窄锥角 + 加径向抖动
   （孩子不落在同一个半径上）后，20 条主枝分开了，才像放射决策树。
3. ② 一开始用 `gl.readPixels` 数亮像素，读回来是 0。不是画面空，是没开 `preserveDrawingBuffer` 时
   在 rAF 之外读默认帧缓冲本来就是空的 —— 量法的问题。改成零依赖解 CDP 截下来的 PNG 数真实像素。
